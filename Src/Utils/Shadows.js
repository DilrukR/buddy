import { StyleSheet } from "react-native";

const Custom_shadow = StyleSheet.create({
  shadow: {
    shadowColor: "#7F5DF0",
    borderRadius: 24,
    shadowOffset: {
      width: 0,
      height: 0,
    },
    shadowOpacity: 0.15,
    shadowRadius: 5,
    elevation: 2,
  },
});

export default Custom_shadow;
